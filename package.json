{
  "name": "electron-codepen-omni",
  "version": "1.0.0",
  "description": "Inject some CSS to customize Codepen colors ;)",
  "main": "src/main/app.js",
  "scripts": {
    "start": "electron .",
    "build": "npx electron-packager . --icon assets/icon.icns --out=packages",
    "build-win64": "npx electron-packager . Codepen --overwrite --out=packages --platform=win32 --arch=x64 app-version=1.0 --icon=assets/icon.ico"
  },
  "devDependencies": {
    "electron": "^15"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
